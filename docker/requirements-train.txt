# Pinned dependencies for the training image (T7).
# All three model trainers (persistence, xgboost, lstm) run from this image.
#
# torch: no `+cpu` suffix. On linux/arm64 PyPI ships only CPU wheels (no CUDA),
# so the plain pin is CPU-only by construction. On linux/amd64 the same pin
# would pull CUDA — re-pin via the pytorch CPU index if amd64 support is
# needed (`--extra-index-url https://download.pytorch.org/whl/cpu` and
# `torch==2.6.0+cpu` or newer, which is when arm64 `+cpu` wheels first ship).
numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0
PyYAML==6.0.2
python-dateutil==2.9.0.post0
scikit-learn==1.5.2
pvlib==0.11.0
xgboost==2.1.3
torch==2.4.1
mlflow==2.18.0
boto3==1.35.74
