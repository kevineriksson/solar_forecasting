# Pinned dependencies for the serving image (T7).
# FastAPI + uvicorn loading the Production model from MLflow. CPU-only.
# When the Production model is an LSTM (T6), add `torch==2.4.1+cpu` from the
# pytorch CPU index — kept out by default to honor the < 1 GB budget.
numpy==1.26.4
pandas==2.2.3
PyYAML==6.0.2
pvlib==0.11.0
scikit-learn==1.5.2
xgboost==2.1.3
mlflow==2.18.0
boto3==1.35.74
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
prometheus-client==0.21.1
